image:
  repository: "<your-acr-login-server>/secure-aks-service"
  digest: "sha256:replace-with-dev-image-digest"
  pullPolicy: IfNotPresent

workloadIdentity:
  enabled: true
  serviceAccountName: secure-service
  clientID: "<dev-workload-identity-client-id>"

monitoring:
  enabled: true
  serviceMonitor:
    enabled: true
  prometheusRule:
    enabled: true

